# MariaDB (default)

Tables are tab<DocType>. Do not UPDATE them by hand on production. Learn SELECT, JOIN, WHERE, GROUP BY, utf8mb4, InnoDB.

## Learn

- @docs@ MariaDB SELECT | https://mariadb.com/kb/en/select/
- @official@ Database API | https://docs.frappe.io/framework/user/en/api/database
